# Project Debug Rules (Non-Obvious Only)

- Development server runs on port 3000 (next.config.ts)
- Docker containers are configured in docker-compose.yml
- Backend API runs on port 5000 (ServerApp)
- Next.js build artifacts are stored in .next/ directory
- Environment variables are configured in .env files