version: '3.9'

services:
  api:
    build:
      context: ../ServerApp
    ports:
      - "5000:8080"

  frontend:
    build:
      context: ../clientapp
    ports:
      - "3000:3000"
    depends_on:
      - api

